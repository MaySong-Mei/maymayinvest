# Reviewer evaluation — prompt v1-2026-05-22

**Date run**: 2026-05-23
**Prompt version**: `v1-2026-05-22`
**Eval set**: `v1/backend/tests/eval/reviewer_eval_set.py` (5 hand-labeled fixtures)
**Driver**: `v1/backend/scripts/eval_reviewer.py`
**Raw output**: `reviewer-v1-2026-05-22-2026-05-23-results.json`

## Headline finding

**The eval did not measure reviewer quality.** All 5 cases failed at the subprocess plumbing layer with `claude` exit code 1 after ~3 seconds. No reviewer verdicts were produced.

What this DOES tell us is significantly more important than what we set out to measure:

> **A `claude -p` subprocess spawned from inside a Claude Code session does not inherit the parent's authentication. It runs as if no user is logged in, gets 401 Invalid authentication credentials, and exits 1.**

This invalidates a foundational assumption of `ClaudeCodeReviewer`'s design: that spawning a subprocess would "just work" by piggybacking on the operator's existing Claude Code subscription.

## Result table

| case | expected | actual | latency | error |
|---|---|---|---|---|
| case-1-clear-right-bet | right_bet | (subprocess failed) | 2.7s | exit 1 |
| case-2-clear-wrong-bet | wrong_bet | (subprocess failed) | 2.4s | exit 1 |
| case-3-legitimately-ambiguous | ambiguous | (subprocess failed) | 2.3s | exit 1 |
| case-4-adversarial-polished-bad-bet | wrong_bet | (subprocess failed) | 3.3s | exit 1 |
| case-5-correct-no-action | right_bet | (subprocess failed) | 3.1s | exit 1 |

**matched: 0/5    errors: 5    avg latency: 2.8s**

## Root cause investigation

After the eval failed, a direct probe (`echo "say hi" | claude -p --output-format text`) reproduced the issue:

```
Failed to authenticate. API Error: 401 Invalid authentication credentials
exit 1
```

Reading `claude --help` reveals two relevant facts:

1. Default `claude` (no `--bare`) uses **OAuth + system keychain** for authentication.
2. The `--bare` mode documents authentication explicitly: *"Anthropic auth is strictly ANTHROPIC_API_KEY or apiKeyHelper via --settings (OAuth and keychain are never read)."*

By process of elimination, when `claude` is invoked from a non-interactive subprocess context with no `ANTHROPIC_API_KEY` env var, the keychain lookup fails silently — the parent Claude Code session's OAuth tokens are not exposed to child processes.

## What this means for the architecture

`ClaudeCodeReviewer` was designed around the assumption that you could "use Claude Code as a backend" by spawning `claude -p` subprocesses. **In practice that assumption requires either:**

- (a) The user holds an Anthropic API key (`ANTHROPIC_API_KEY`) and passes it through, OR
- (b) Some yet-undiscovered way to surface the Claude Code session's OAuth credentials to a subprocess

(a) is workable but changes the cost model: API calls are token-priced, not subscription-included.
(b) was never documented and may not exist by design (security boundary).

This is exactly the failure mode `PHILOSOPHY.md` and the original "Why this is weird" analysis warned about, materializing earlier than expected:

> *"... 'Claude Code is a developer tool, not a 7×24 daemon service.' Deployment to an unattended server is hard (OAuth tokens, keychain, login persistence)."*

We didn't even need to get to "deploy to a server" — even **same-machine subprocess** doesn't carry auth.

## Subordinate bug found

`ClaudeCodeReviewer.review()` reports stderr in the error message when subprocess returncode != 0. But the 401 error message comes out on **stdout**, not stderr. The error string in the reviewer's ReviewerError currently reads `stderr=''` even though the diagnostic info exists in stdout. Should be fixed to include both streams, with stderr first, stdout fallback.

## Implication for the project

The plan was: write `ClaudeCodeReviewer`, run it, see whether prompt v1 produces useful judgments. We learned a different, more upstream answer:

> **`ClaudeCodeReviewer` as currently designed cannot run on this machine without further work.**

Concrete options going forward:

1. **Pivot to `ClaudeAPIReviewer`** (direct Anthropic SDK with API key). This is what `V1_SCOPE.md` calls the "Stage 2" implementation but it has just become Stage 0's only viable path. Requires `ANTHROPIC_API_KEY`.

2. **Investigate `--bare` + `apiKeyHelper` in `--settings`**. Possibly there's a way to give `claude -p` a credential pathway without an API key. Low probability, but worth one more probe.

3. **Discover and use the Claude Code session's OAuth bridge** (if one exists). Likely no public-facing way to do this.

4. **Reviewer is a different LLM entirely** — e.g., a local Ollama model for outcome-blind review. Cheap, but quality unknown.

The eval is **not a reflection on reviewer prompt quality**. The prompt was never exercised. We will need to re-run this eval against whichever Stage 0 reviewer implementation succeeds option (1) / (2) / (3) / (4).

## What this eval IS evidence of

- The eval set itself loads cleanly, the fixtures are well-formed, the driver correctly handles failure modes (it captured errors instead of pretending verdicts happened).
- The architectural assumption "spawn claude subprocess → inherit auth" is false. This is now a documented constraint.
- The operator's earlier critique of the subprocess approach ("Why this is weird") was correct in a way that materializes earlier than predicted.

## Notes on operator process (meta)

The reviewer subagent (commit `0a0dfe9`) flagged that running the reviewer against StubAnalyzer dossiers wouldn't measure what we needed. The pivot to hand-labeled fixtures (option 5) was made on that basis. The hand-labeled fixtures are still useful — they will exercise whatever reviewer implementation we end up with. **They didn't get exercised here because the subprocess never reached the LLM call**, but the work is not wasted.

The honest read of this session:
- 1 commit shipped (PHILOSOPHY.md goal-evolution invariants)
- 1 commit pending (this eval analysis + eval driver + fixtures + the stdout bug fix)
- The empirical question we set out to answer ("does the reviewer make useful judgments?") is still unanswered.
- One question we didn't set out to answer ("does `claude -p` from subprocess work?") is now answered: no.

The latter answer is more valuable than the former would have been. That's a feature of empirical work, not a bug — but it required us to actually run the thing.

---

## Addendum 2026-05-23: deeper diagnosis after env scrub failed

The user proposed applying the subagent-as-prober pattern (use subagents to test hypotheses, not just to evaluate decisions). Two prober rounds followed.

### Round 1: ANTHROPIC_API_KEY="" poisoning hypothesis

A prober subagent (agent `afe730eaf7c9dc246`) inspected env + Claude Code config and found:
- Parent CC session exports `ANTHROPIC_API_KEY=""` (empty string, not unset)
- claude auth chain prioritizes `ANTHROPIC_API_KEY` over OAuth keychain
- Empty string in env → child sends empty bearer token → 401

This generated a clean, testable hypothesis: **scrub the empty env var, OAuth keychain should take over**.

We implemented the scrub in `app/intel/reviewer/claude_code.py` (`_build_subprocess_env`) and re-tested with a single case.

**Result**: still 401. H1 partially falsified.

### Round 2: OAuth refresh race hypothesis

A second prober subagent (agent `aebc2ac79dda312e5`) ran a more thorough probe with debug-file capture and found the smoking gun in `--debug-file` output:

- `[ERROR] AxiosError: [url=https://platform.claude.com/v1/oauth/token,status=400]`
- `[ERROR] Error: EPERM: operation not permitted, mkdir 'C:\Users\xmeiy\.claude\.oauth_refresh.lock'`
- `[claudeai-mcp] Missing user:mcp_servers scope (scopes=user:inference,user:profile)`

Sequence of events when `claude -p` runs as a child of an active CC session:

1. Child reads `~/.claude/.credentials.json` successfully (auth state IS loadable)
2. Child detects access token expired
3. Child attempts to refresh via `https://platform.claude.com/v1/oauth/token`
4. Refresh fails with 400 — **the parent CC session is also continuously refreshing the same OAuth token, and the refresh_token has already been rotated by the parent**
5. Child tries to acquire `.oauth_refresh.lock` to serialize, but `mkdir` returns EPERM (Windows-specific quirk under nested process invocation)
6. Without a fresh access token, the actual API call returns 401

The `CLAUDE_CODE_SDK_HAS_HOST_AUTH_REFRESH=1` and `CLAUDE_CODE_SDK_HAS_OAUTH_REFRESH=1` env vars (exported by the parent) are documented signals meaning "host process handles refresh out-of-band". The child sees them but still attempts its own refresh — possibly a bug, possibly intentional because the child doesn't trust an unknown parent. Either way, in practice it racing the parent for the same token guarantees failure.

### Root cause (H9)

**OAuth-with-shared-refresh-token is architecturally incompatible with nested `claude` subprocess invocation.**

This is independent of:
- env var hygiene (we tried clean env, still fails)
- binary version (both 2.1.143 and 2.1.148 produce the same 401)
- per-install credentials (there is only one credentials store; both binaries read it)
- prompt format (positional arg vs stdin pipe — same failure)

### What this means architecturally

`ClaudeCodeReviewer` as currently designed cannot run inside a Claude Code session **with the user's interactive OAuth credentials**. The OAuth refresh dance with the parent process is fundamentally racy.

**Sanctioned escape hatch (per Anthropic docs)**: run `claude setup-token` once from a standalone terminal (NOT nested under a Claude Code session). It mints a long-lived subscription token that bypasses the OAuth refresh dance entirely. The resulting token gets set as `ANTHROPIC_API_KEY` (or via apiKeyHelper) and is **safe to share between parent CC and child `claude -p` because it's not refresh-rotating**.

This requires the user (human) to run that command in an external terminal. The operator (Claude Code) cannot do it from inside itself — that would just create another nested context with the same problem.

### Updated forward path

Stage 0 reviewer paths, refined:

| path | viability | requires |
|---|---|---|
| ClaudeCodeReviewer via OAuth (original design) | **NOT VIABLE** | impossible from nested CC session due to refresh race |
| ClaudeCodeReviewer via setup-token | viable | user runs `claude setup-token` externally once; token stored somewhere CC reviewer can read |
| ClaudeAPIReviewer via ANTHROPIC_API_KEY | viable | user has API key (separate from subscription) |
| Reviewer via different LLM (local Ollama, etc) | viable but quality unknown | local model installation |

The `setup-token` path is novel and underrated — it gives subscription users the headless-token model normally reserved for API customers, no extra billing. It's what the codebase should target first if the user has a Pro/Max subscription.

### Process meta-finding

This subagent-as-prober loop is now a documented practice (see PHILOSOPHY.md updates in subsequent commit if any). Cost was ~2 prober invocations + ~10 minutes wall time, and it eliminated multiple weeks of dead-end work pursuing OAuth solutions that fundamentally cannot work.

The operator's confidence was 0.85 going into the env-scrub experiment (single change, expected to fix). Result: H1 falsified. **The right read of this is not "operator was overconfident"** — H1 was a serious hypothesis with concrete supporting evidence. It was the **cheapest** hypothesis to test (literally one line of code). The fact that it didn't work narrowed the search rapidly. The second prober had the benefit of the first round's negative evidence and used that to find H9.

This is what empirical work looks like. Each failed hypothesis tightens the bound on the true cause. The 401 messages were costly only in API budget terms (5 + 1 + 1 = 7 claude -p calls); architectural understanding gain dwarfs that cost.

### Code state at end of session

- `app/intel/reviewer/claude_code.py` has `_build_subprocess_env` (scrubs empty `ANTHROPIC_API_KEY`). Kept — it's correct, just insufficient on its own. When `setup-token` path is implemented, this scrub still helps.
- `app/intel/reviewer/claude_code.py` reports both stdout and stderr in subprocess errors. Kept (the 401 message lives on stdout).
- 5 hand-labeled fixtures in `tests/eval/reviewer_eval_set.py` and `scripts/eval_reviewer.py` remain ready to use; they will exercise whatever Stage 0 reviewer implementation finally works.
- 56/56 unit tests still pass.

### Open question for the user

Three forward paths require the user's input:
1. Run `claude setup-token` externally → reviewer continues at near-zero ongoing cost
2. Obtain `ANTHROPIC_API_KEY` → switch to ClaudeAPIReviewer (also Stage 2 implementation; pull-forward)
3. Defer reviewer experiment, build non-reviewer modules (promote_signal, EDGAR poller, risk gate) and revisit when option 1 or 2 is acquired

Operator recommendation: option 1, because it's free, sanctioned, and keeps the project on the CC-as-operator framing. But this is user's call — it involves a side-channel action only the user can take.

---

## Addendum 2026-05-23 (later): Path 1 unblocked, eval ran successfully

User ran `claude setup-token` externally, set `CLAUDE_CODE_OAUTH_TOKEN` via `setx`. The token propagated to a fresh git-bash shell and to Python subprocesses spawned from that shell. With `_build_subprocess_env` Path 1 active (strips empty ANTHROPIC_API_KEY, forwards OAuth token), the 5-case eval ran clean.

### Headline result

**4/5 cases matched expected verdict. 0 errors. Avg latency 24.6s.**

| case | expected | actual | match | latency | reviewer confidence |
|---|---|---|---|---|---|
| 1 clear right_bet | right_bet | right_bet | ✅ | 22.3s | 0.82 |
| 2 clear wrong_bet | wrong_bet | wrong_bet | ✅ | 29.6s | 0.90 |
| 3 legitimately ambiguous | ambiguous | **right_bet** | ❌ | 21.6s | 0.82 |
| 4 adversarial polished-bad-bet | wrong_bet | wrong_bet | ✅ | 28.0s | 0.78 |
| 5 correct no-action | right_bet | right_bet | ✅ | 21.4s | 0.85 |

Raw output: `reviewer-v1-2026-05-22-2026-05-23-results.json`.

### Substantive findings

**1. Reviewer prompt v1 is viable**. Four cases hit ground truth with substantive, defensible reasoning. Latency is well inside the 30-90s right-side trading budget. Flag taxonomy matches expectations.

**2. Reviewer is robust to surface polish (the adversarial test)**. Case 4 was hand-crafted with polished-sounding reasoning citing an invented "post-9PM FDA filings underperform" pattern, surfaced through an invented `fda_timing_anomaly` skill. Reviewer not only verdicted wrong_bet, it produced this analysis (excerpted):

> "The reasoning chain rests almost entirely on a single claimed statistical pattern... Nothing in the available_info_snapshot substantiates this pattern: there is no sample size, no base rate, no confidence interval... The operator treats the skill output as a 'green light' rather than as one input, which is a classic case of skill-output laundering."
>
> "The operator is shorting INTO a fundamentally positive, hard-catalyst event... Positive Phase 3 readouts are among the highest-variance, most-gap-prone events in equities, and the asymmetry is famously skewed against shorts: the realistic downside on a short is a multi-hundred-percent gap on partnership/buyout/secondary news, while the upside is capped at the fade magnitude (~2-4% per the operator's own claim). The reasoning chain never engages with this asymmetry..."

The reviewer additionally raised flags the operator's eval set didn't predict, both more precise than the operator's hints:
- `ignored_asymmetric_tail_risk` (operator had used `missed_obvious_downside_catalyst`)
- `skill_output_treated_as_conclusion_not_input` (operator had used `skills_invoked_inappropriately`)

These are better names for what the operator was hand-flagging. Future fixtures and the reviewer prompt's flag taxonomy should probably absorb them.

**3. Reviewer flagged a fixture defect the operator hadn't noticed**. Case 1's "clear right_bet" dossier proposed a market order in extended-hours liquidity, while the reasoning chain itself complained about poor extended-hours liquidity. Reviewer raised `order_type_mildly_inconsistent_with_liquidity_concern`. This is a small defect — reviewer still verdicted right_bet at 0.82 confidence — but it demonstrates a useful property: **the reviewer found an issue the operator missed during fixture authoring**. The same will happen on real dossiers.

**4. Case 3 disagreement is label-side, not reviewer-side**. Reviewer verdicted `right_bet` for a dossier the operator labeled `ambiguous`. Reviewer's reasoning (excerpted):

> "The operator faces a genuinely ambiguous signal... The reasoning correctly identifies the core epistemic problem... The operator does not pretend to know more than they do. The alternatives considered are appropriate and the rejections are well-justified."
>
> "Process-wise: information was correctly identified as insufficient, alternatives were weighed, the rejection of the probe was honest rather than convenient, and the confidence matched the reasoning strength. **This is a right bet.**"

Reviewer's logic: "ambiguous information + sound process + honest acknowledgment of uncertainty = right bet, not ambiguous." This is a stronger position than the operator's. The fixture's own rationale text already conceded: *"A reviewer who verdicts right_bet is also defensible (process matches information)."*

The "FAIL" here is the operator's labeling being uncertain, not the reviewer's judgment being wrong. The reviewer is **disambiguating a category the operator hadn't fully thought through**: the difference between "the situation is ambiguous" (which is about the world) and "the operator's bet is ambiguous" (which is about decision quality). The reviewer applies the latter, correctly: a sound decision under ambiguity is a right bet, not an ambiguous one. The `ambiguous` verdict should be reserved for cases where the reviewer themselves is unsure whether the process was sound.

This is a useful concept correction. It surfaces a **goal-side calibration question**: are the operator's labels meaningful, or are they reflecting a confused taxonomy? More on this in PHILOSOPHY.md "Goal state machine" addition.

### Implications for the project

- ClaudeCodeReviewer + OAuth path is now a working component, not a hypothesis
- Reviewer prompt v1 should be promoted from "experimental" to "operational" once we have one more eval run (5 cases is small)
- The flag taxonomy in `prompt.py` is too narrow — extending it with the reviewer's invented flag names is a candidate proposal
- The operator's eval-set labels need a calibration pass — case 3 in particular needs its `expected_verdict` reconsidered (this is itself a `proposals/` candidate, not a silent fixture edit)
- The "hardest unsolved problem" identified in PHILOSOPHY.md is now partially answered: in this controlled setting, reviewer judgments appear well-grounded. Open question: does this hold up on real (non-hand-crafted) dossiers and across more cases?

### Cost of this experiment

5 successful claude -p invocations × ~25s × Pro/Max subscription credit. Plus the 7 failed invocations during the auth investigation. Total subscription budget consumed: small (well under 1% of monthly Pro/Max quota). Engineering value: high — the entire ClaudeCodeReviewer module is now empirically validated end-to-end.

### What we know now that we didn't before

- Reviewer prompt v1 can produce useful verdicts on hand-crafted fixtures
- Reviewer can resist polished-but-bad reasoning (the adversarial case)
- Reviewer surfaces flags the operator missed (both fixture defects and gaps in the operator's flag taxonomy)
- The `_build_subprocess_env` Path 1 design works correctly in production
- The OAuth-token setup-token path is viable for headless subscription use
- Reviewer disagreement isn't always reviewer error — sometimes it's the operator's label that's wrong
